# Claude Agent HTTP dependencies

# Core
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Claude Agent SDK
claude-agent-sdk>=0.1.0

# Storage
aiosqlite>=0.19.0
asyncpg>=0.29.0      # PostgreSQL support (optional)

# Configuration
pyyaml>=6.0

# SSE streaming
sse-starlette>=1.6.0
