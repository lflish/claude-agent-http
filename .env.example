# ============================================
# Claude Agent HTTP - Environment Configuration
# ============================================
# Copy this file to .env: cp .env.example .env

# ============================================
# Anthropic API Configuration
# ============================================
# Choose ONE authentication method:

# Method 1: Official Anthropic API (Standard)
# Get your key from: https://console.anthropic.com/
# ANTHROPIC_API_KEY=your_api_key_here

# Method 2: Custom API Endpoint (e.g., Proxy, Self-hosted)
# ANTHROPIC_BASE_URL=https://your-custom-endpoint.com
# ANTHROPIC_AUTH_TOKEN=your_auth_token_here

# Optional: Override model
# ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# ============================================
# Deployment Mode (Optional)
# ============================================
# Storage backend: sqlite (default) | postgresql | memory
CLAUDE_AGENT_SESSION_STORAGE=sqlite

# Docker user/group IDs (optional, defaults to 1000:1000)
# For bind mounts, set to match your host user:
# UID=$(id -u)
# GID=$(id -g)

# ============================================
# Bind Mounts Configuration (Optional)
# ============================================
# Only needed if using docker-compose.override.bindmounts.yml
# HOST_CLAUDE_DATA_DIR=/opt/claude-code-http/claude-data
# HOST_USER_DATA_DIR=/opt/claude-code-http/claude-users

# ============================================
# PostgreSQL Configuration (Optional)
# ============================================
# Only needed when CLAUDE_AGENT_SESSION_STORAGE=postgresql
# CLAUDE_AGENT_SESSION_PG_DATABASE=claude_agent
# CLAUDE_AGENT_SESSION_PG_USER=postgres
# CLAUDE_AGENT_SESSION_PG_PASSWORD=postgres

# ============================================
# Advanced Configuration (Optional)
# ============================================
# API_PORT=8000
# CLAUDE_AGENT_SESSION_TTL=3600
# CLAUDE_AGENT_USER_BASE_DIR=/data/claude-users
# HOST_CONFIG_FILE=./config.yaml

# ============================================
# Quick Start Guide
# ============================================
# 1. Configure API authentication above (choose one method)
#
# 2. Choose deployment mode:
#    SQLite (default):     docker-compose up -d
#    PostgreSQL:           docker-compose --profile postgres up -d
#    Bind mounts:          See docker-compose.override.bindmounts.yml
#
# 3. Test: curl http://localhost:8000/health
#
# For detailed documentation, see DOCKER.md
